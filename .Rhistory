for(r in 1:n_rings){
draw_ring(outters[r],inners[r])
}
}
svg('center_logo.svg',width=5,height=5)
par(mar=rep(2,4))
make_logo()
dev.off()
rm(list=ls())
phi <- (1+5^(1/2))/2
to_cartesian <- function(radius,angle) {
x <- radius*cos(angle)
y <- radius*sin(angle)
return(list(x=x,y=y))
}
draw_ring <- function(outter_radius,inner_radius) {
theta <- seq(0,2*pi,length.out=160)
outter <- to_cartesian(outter_radius,theta)
inner <- to_cartesian(inner_radius,theta)
polygon(x=c(outter$x,inner$x[length(inner$x):1]),
y=c(outter$y,inner$y[length(inner$y):1]),
col='blue',border=NA)
}
make_logo <- function(n_rings=9) {
# Generate rings coordinates
outters <- 1
for (i in 2:(n_rings+1)) {
outters[i] <- outters[(i-1)]/phi
}
inners <- NULL
for(j in 1:n_rings){
inners[j] <- outters[j]-((outters[j]-outters[(j+1)])/phi)
}
# Make plto
plot(0,type='n',ylim=c(-1,1),xlim=c(-1,1),
ann=F)
for(r in 1:n_rings){
draw_ring(outters[r],inners[r])
}
}
svg('center_logo.svg',width=5,height=5)
par(mar=rep(2,4))
make_logo()
dev.off()
rm(list=ls())
phi <- (1+5^(1/2))/2
to_cartesian <- function(radius,angle) {
x <- radius*cos(angle)
y <- radius*sin(angle)
return(list(x=x,y=y))
}
draw_ring <- function(outter_radius,inner_radius) {
theta <- seq(0,2*pi,length.out=160)
outter <- to_cartesian(outter_radius,theta)
inner <- to_cartesian(inner_radius,theta)
polygon(x=c(outter$x,inner$x[length(inner$x):1]),
y=c(outter$y,inner$y[length(inner$y):1]),
col='blue',border=NA)
}
make_logo <- function(n_rings=100) {
# Generate rings coordinates
outters <- 1
for (i in 2:(n_rings+1)) {
outters[i] <- outters[(i-1)]/phi
}
inners <- NULL
for(j in 1:n_rings){
inners[j] <- outters[j]-((outters[j]-outters[(j+1)])/phi)
}
# Make plto
plot(0,type='n',ylim=c(-1,1),xlim=c(-1,1),
ann=F)
for(r in 1:n_rings){
draw_ring(outters[r],inners[r])
}
}
svg('center_logo.svg',width=5,height=5)
par(mar=rep(2,4))
make_logo()
dev.off()
rm(list=ls())
phi <- (1+5^(1/2))/2
to_cartesian <- function(radius,angle) {
x <- radius*cos(angle)
y <- radius*sin(angle)
return(list(x=x,y=y))
}
draw_ring <- function(outter_radius,inner_radius) {
theta <- seq(0,2*pi,length.out=160)
outter <- to_cartesian(outter_radius,theta)
inner <- to_cartesian(inner_radius,theta)
polygon(x=c(outter$x,inner$x[length(inner$x):1]),
y=c(outter$y,inner$y[length(inner$y):1]),
col='blue',border=NA)
}
make_logo <- function(n_rings=20) {
# Generate rings coordinates
outters <- 1
for (i in 2:(n_rings+1)) {
outters[i] <- outters[(i-1)]/phi
}
inners <- NULL
for(j in 1:n_rings){
inners[j] <- outters[j]-((outters[j]-outters[(j+1)])/phi)
}
# Make plto
plot(0,type='n',ylim=c(-1,1),xlim=c(-1,1),
ann=F)
for(r in 1:n_rings){
draw_ring(outters[r],inners[r])
}
}
svg('center_logo.svg',width=5,height=5)
par(mar=rep(2,4))
make_logo()
dev.off()
rm(list=ls())
phi <- (1+5^(1/2))/2
to_cartesian <- function(radius,angle) {
x <- radius*cos(angle)
y <- radius*sin(angle)
return(list(x=x,y=y))
}
draw_ring <- function(outter_radius,inner_radius) {
theta <- seq(0,2*pi,length.out=160)
outter <- to_cartesian(outter_radius,theta)
inner <- to_cartesian(inner_radius,theta)
polygon(x=c(outter$x,inner$x[length(inner$x):1]),
y=c(outter$y,inner$y[length(inner$y):1]),
col='blue',border=NA)
}
make_logo <- function(n_rings=20) {
# Generate rings coordinates
outters <- 1
for (i in 2:(n_rings+1)) {
outters[i] <- outters[(i-1)]/phi
}
inners <- NULL
for(j in 1:n_rings){
inners[j] <- outters[j]-((outters[j]-outters[(j+1)])/phi)
}
# Make plto
plot(0,type='n',ylim=c(-1,1),xlim=c(-1,1),
ann=F,axes=F)
for(r in 1:n_rings){
draw_ring(outters[r],inners[r])
}
}
svg('center_logo.svg',width=5,height=5)
par(mar=rep(2,4))
make_logo()
dev.off()
rm(list=ls())
phi <- (1+5^(1/2))/2
to_cartesian <- function(radius,angle) {
x <- radius*cos(angle)
y <- radius*sin(angle)
return(list(x=x,y=y))
}
draw_ring <- function(outter_radius,inner_radius) {
theta <- seq(0,2*pi,length.out=160)
outter <- to_cartesian(outter_radius,theta)
inner <- to_cartesian(inner_radius,theta)
polygon(x=c(outter$x,inner$x[length(inner$x):1]),
y=c(outter$y,inner$y[length(inner$y):1]),
col='blue',border=NA)
}
make_logo <- function(n_rings=20) {
# Generate rings coordinates
outters <- 1
for (i in 2:(n_rings+1)) {
outters[i] <- outters[(i-1)]/phi
}
inners <- NULL
for(j in 1:n_rings){
inners[j] <- outters[j]-((outters[j]-outters[(j+1)])/phi)
}
# Make plto
plot(0,type='n',ylim=c(-1,1),xlim=c(-1,1),
ann=F,axes=F)
for(r in 1:n_rings){
draw_ring(outters[r],inners[r])
}
}
svg('center_logo.svg',width=5,height=5)
par(mar=rep(0,4))
make_logo()
dev.off()
rm(list=ls())
phi <- (1+5^(1/2))/2
to_cartesian <- function(radius,angle) {
x <- radius*cos(angle)
y <- radius*sin(angle)
return(list(x=x,y=y))
}
draw_ring <- function(outter_radius,inner_radius) {
theta <- seq(0,2*pi,length.out=160)
outter <- to_cartesian(outter_radius,theta)
inner <- to_cartesian(inner_radius,theta)
polygon(x=c(outter$x,inner$x[length(inner$x):1]),
y=c(outter$y,inner$y[length(inner$y):1]),
col='blue',border=NA)
}
make_logo <- function(n_rings=20) {
# Generate rings coordinates
outters <- 1
for (i in 2:(n_rings+1)) {
outters[i] <- outters[(i-1)]/phi
}
inners <- NULL
for(j in 1:n_rings){
inners[j] <- outters[j]-((outters[j]-outters[(j+1)])/phi)
}
# Make plto
plot(0,type='n',ylim=c(-1,1),xlim=c(-1,1),
ann=F,axes=F)
for(r in 1:n_rings){
draw_ring(outters[r],inners[r])
}
}
svg('center_logo.svg',width=5,height=5)
par(mar=rep(1,4))
make_logo()
dev.off()
rm(list=ls())
phi <- (1+5^(1/2))/2
to_cartesian <- function(radius,angle) {
x <- radius*cos(angle)
y <- radius*sin(angle)
return(list(x=x,y=y))
}
draw_ring <- function(outter_radius,inner_radius) {
theta <- seq(0,2*pi,length.out=160)
outter <- to_cartesian(outter_radius,theta)
inner <- to_cartesian(inner_radius,theta)
polygon(x=c(outter$x,inner$x[length(inner$x):1]),
y=c(outter$y,inner$y[length(inner$y):1]),
col='blue',border=NA)
}
make_logo <- function(n_rings=20) {
# Generate rings coordinates
outters <- 1
for (i in 2:(n_rings+1)) {
outters[i] <- outters[(i-1)]/phi
}
inners <- NULL
for(j in 1:n_rings){
inners[j] <- outters[j]-((outters[j]-outters[(j+1)])/phi)
}
# Make plto
plot(0,type='n',ylim=c(-1,1),xlim=c(-1,1),
ann=F,axes=F)
for(r in 1:n_rings){
draw_ring(outters[r],inners[r])
}
}
svg('center_logo.svg',width=5,height=5)
par(mar=rep(1.5,4))
make_logo()
dev.off()
rm(list=ls())
phi <- (1+5^(1/2))/2
to_cartesian <- function(radius,angle) {
x <- radius*cos(angle)
y <- radius*sin(angle)
return(list(x=x,y=y))
}
draw_ring <- function(outter_radius,inner_radius) {
theta <- seq(0,2*pi,length.out=160)
outter <- to_cartesian(outter_radius,theta)
inner <- to_cartesian(inner_radius,theta)
polygon(x=c(outter$x,inner$x[length(inner$x):1]),
y=c(outter$y,inner$y[length(inner$y):1]),
col='blue',border=NA)
}
make_logo <- function(n_rings=20) {
# Generate rings coordinates
outters <- 1
for (i in 2:(n_rings+1)) {
outters[i] <- outters[(i-1)]/phi
}
inners <- NULL
for(j in 1:n_rings){
inners[j] <- outters[j]-((outters[j]-outters[(j+1)])/phi)
}
# Make plto
plot(0,type='n',ylim=c(-1,1),xlim=c(-1,1),
ann=F,axes=F)
for(r in 1:n_rings){
draw_ring(outters[r],inners[r])
}
}
svg('center_logo.svg',width=5,height=5)
par(mar=rep(.5,4))
make_logo()
dev.off()
rm(list=ls())
phi <- (1+5^(1/2))/2
to_cartesian <- function(radius,angle) {
x <- radius*cos(angle)
y <- radius*sin(angle)
return(list(x=x,y=y))
}
draw_ring <- function(outter_radius,inner_radius) {
theta <- seq(0,2*pi,length.out=160)
outter <- to_cartesian(outter_radius,theta)
inner <- to_cartesian(inner_radius,theta)
polygon(x=c(outter$x,inner$x[length(inner$x):1]),
y=c(outter$y,inner$y[length(inner$y):1]),
col='blue',border=NA)
}
make_logo <- function(n_rings=10) {
# Generate rings coordinates
outters <- 1
for (i in 2:(n_rings+1)) {
outters[i] <- outters[(i-1)]/phi
}
inners <- NULL
for(j in 1:n_rings){
inners[j] <- outters[j]-((outters[j]-outters[(j+1)])/phi)
}
# Make plto
plot(0,type='n',ylim=c(-1,1),xlim=c(-1,1),
ann=F,axes=F)
for(r in 1:n_rings){
draw_ring(outters[r],inners[r])
}
}
svg('center_logo.svg',width=5,height=5)
par(mar=rep(.5,4))
make_logo()
dev.off()
rm(list=ls())
phi <- (1+5^(1/2))/2
to_cartesian <- function(radius,angle) {
x <- radius*cos(angle)
y <- radius*sin(angle)
return(list(x=x,y=y))
}
draw_ring <- function(outter_radius,inner_radius) {
theta <- seq(0,2*pi,length.out=160)
outter <- to_cartesian(outter_radius,theta)
inner <- to_cartesian(inner_radius,theta)
polygon(x=c(outter$x,inner$x[length(inner$x):1]),
y=c(outter$y,inner$y[length(inner$y):1]),
col='blue',border=NA)
}
make_logo <- function(n_rings=11) {
# Generate rings coordinates
outters <- 1
for (i in 2:(n_rings+1)) {
outters[i] <- outters[(i-1)]/phi
}
inners <- NULL
for(j in 1:n_rings){
inners[j] <- outters[j]-((outters[j]-outters[(j+1)])/phi)
}
# Make plto
plot(0,type='n',ylim=c(-1,1),xlim=c(-1,1),
ann=F,axes=F)
for(r in 1:n_rings){
draw_ring(outters[r],inners[r])
}
}
svg('center_logo.svg',width=5,height=5)
par(mar=rep(.5,4))
make_logo()
dev.off()
rm(list=ls())
phi <- (1+5^(1/2))/2
to_cartesian <- function(radius,angle) {
x <- radius*cos(angle)
y <- radius*sin(angle)
return(list(x=x,y=y))
}
draw_ring <- function(outter_radius,inner_radius) {
theta <- seq(0,2*pi,length.out=160)
outter <- to_cartesian(outter_radius,theta)
inner <- to_cartesian(inner_radius,theta)
polygon(x=c(outter$x,inner$x[length(inner$x):1]),
y=c(outter$y,inner$y[length(inner$y):1]),
col='blue',border=NA)
}
make_logo <- function(n_rings=9) {
# Generate rings coordinates
outters <- 1
for (i in 2:(n_rings+1)) {
outters[i] <- outters[(i-1)]/phi
}
inners <- NULL
for(j in 1:n_rings){
inners[j] <- outters[j]-((outters[j]-outters[(j+1)])/phi)
}
# Make plto
plot(0,type='n',ylim=c(-1,1),xlim=c(-1,1),
ann=F,axes=F)
for(r in 1:n_rings){
draw_ring(outters[r],inners[r])
}
}
svg('center_logo.svg',width=5,height=5)
par(mar=rep(.5,4))
make_logo()
dev.off()
rm(list=ls())
phi <- (1+5^(1/2))/2
to_cartesian <- function(radius,angle) {
x <- radius*cos(angle)
y <- radius*sin(angle)
return(list(x=x,y=y))
}
draw_ring <- function(outter_radius,inner_radius) {
theta <- seq(0,2*pi,length.out=160)
outter <- to_cartesian(outter_radius,theta)
inner <- to_cartesian(inner_radius,theta)
polygon(x=c(outter$x,inner$x[length(inner$x):1]),
y=c(outter$y,inner$y[length(inner$y):1]),
col='blue',border=NA)
}
make_logo <- function(n_rings11) {
# Generate rings coordinates
outters <- 1
for (i in 2:(n_rings+1)) {
outters[i] <- outters[(i-1)]/phi
}
inners <- NULL
for(j in 1:n_rings){
inners[j] <- outters[j]-((outters[j]-outters[(j+1)])/phi)
}
# Make plto
plot(0,type='n',ylim=c(-1,1),xlim=c(-1,1),
ann=F,axes=F)
for(r in 1:n_rings){
draw_ring(outters[r],inners[r])
}
}
svg('center_logo.svg',width=5,height=5)
par(mar=rep(.5,4))
make_logo()
dev.off()
rm(list=ls())
phi <- (1+5^(1/2))/2
to_cartesian <- function(radius,angle) {
x <- radius*cos(angle)
y <- radius*sin(angle)
return(list(x=x,y=y))
}
draw_ring <- function(outter_radius,inner_radius) {
theta <- seq(0,2*pi,length.out=160)
outter <- to_cartesian(outter_radius,theta)
inner <- to_cartesian(inner_radius,theta)
polygon(x=c(outter$x,inner$x[length(inner$x):1]),
y=c(outter$y,inner$y[length(inner$y):1]),
col='blue',border=NA)
}
make_logo <- function(n_rings=11) {
# Generate rings coordinates
outters <- 1
for (i in 2:(n_rings+1)) {
outters[i] <- outters[(i-1)]/phi
}
inners <- NULL
for(j in 1:n_rings){
inners[j] <- outters[j]-((outters[j]-outters[(j+1)])/phi)
}
# Make plto
plot(0,type='n',ylim=c(-1,1),xlim=c(-1,1),
ann=F,axes=F)
for(r in 1:n_rings){
draw_ring(outters[r],inners[r])
}
}
svg('center_logo.svg',width=5,height=5)
par(mar=rep(.5,4))
make_logo()
dev.off()
rm(list=ls())
phi <- (1+5^(1/2))/2
to_cartesian <- function(radius,angle) {
x <- radius*cos(angle)
y <- radius*sin(angle)
return(list(x=x,y=y))
}
draw_ring <- function(outter_radius,inner_radius) {
theta <- seq(0,2*pi,length.out=160)
outter <- to_cartesian(outter_radius,theta)
inner <- to_cartesian(inner_radius,theta)
polygon(x=c(outter$x,inner$x[length(inner$x):1]),
y=c(outter$y,inner$y[length(inner$y):1]),
col='blue',border=NA)
}
make_logo <- function(n_rings=11) {
# Generate rings coordinates
outters <- 1
for (i in 2:(n_rings+1)) {
outters[i] <- outters[(i-1)]/phi
}
inners <- NULL
for(j in 1:n_rings){
inners[j] <- outters[j]-((outters[j]-outters[(j+1)])/phi)
}
# Make plto
plot(0,type='n',ylim=c(-1,1),xlim=c(-1,1),
ann=F,axes=F)
for(r in 1:n_rings){
draw_ring(outters[r],inners[r])
}
}
svg('center_logo.svg',width=5,height=5)
par(mar=rep(.5,4))
make_logo()
dev.off()
